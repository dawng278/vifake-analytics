# ViFake Analytics - Deployment Requirements (lightweight, MOCK_MODE=true)
# For full ML stack (torch, transformers, CLIP, etc.), use: pip install -r requirements-ml.txt

# === Web Framework ===
fastapi==0.104.0
uvicorn[standard]==0.24.0
pydantic==2.5.0
python-multipart==0.0.6
jinja2==3.1.2

# === HTTP ===
requests==2.31.0
aiohttp==3.9.0
httpx==0.25.2

# === Database Clients ===
pymongo==4.6.0
neo4j==5.15.0
redis==5.0.1

# === Authentication & Security ===
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# === Monitoring ===
prometheus-client==0.19.0
structlog==23.2.0
sentry-sdk==1.38.0

# === Utilities ===
python-dotenv==1.0.0
tqdm==4.66.1
python-dateutil==2.8.2
pytz==2023.3

# === Video Processing ===
yt-dlp>=2024.1.0          # Extract audio/frame từ TikTok URL
openai-whisper>=20231117  # Transcribe audio → text
ffmpeg-python>=0.2.0      # Wrapper Python cho ffmpeg
aiofiles>=23.0            # Async file I/O

# === AI Audio Detection ===
librosa>=0.10.1           # Audio analysis: MFCC, spectrograms
scikit-learn>=1.3.0        # ML classifiers for voice spoofing detection
# resemblyzer==0.1.4      # Optional: voice embedding (max version is 0.1.4, not used in current code)

# === AI Vision Detection ===
# efficientnet-pytorch     # Optional: requires torch/CUDA — use requirements-ml.txt instead
# face-recognition         # Optional: requires dlib — code uses opencv as fallback
opencv-python>=4.8.1      # Image processing for face crops (used by face_ai_detector.py)

# === Lightweight ML (no torch/CUDA) ===
numpy>=1.26.0
