# ViFake Analytics — Lightweight deploy requirements
# Dùng file này cho Render / Railway / Koyeb (free tier, RAM <= 512MB)
# Rule-based NLP fallback hoạt động không cần torch/transformers
# XGBoost fusion load từ file .joblib nếu có, fallback sang rule-based nếu không

# === Web Framework ===
fastapi>=0.115.0,<1.0.0
uvicorn[standard]>=0.30.0,<1.0.0
pydantic>=2.10.0,<3.0.0
python-multipart==0.0.6

# === HTTP ===
requests==2.31.0
httpx==0.25.2
aiohttp==3.9.0
aiofiles>=23.0

# === Authentication & Security ===
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# === Monitoring ===
structlog==23.2.0

# === Utilities ===
python-dotenv==1.0.0
python-dateutil==2.8.2
pytz==2023.3

# === Lightweight ML (no torch) ===
numpy>=1.24.0
pandas>=2.0.0
xgboost>=2.0.0
scikit-learn>=1.3.0
joblib>=1.3.0
Pillow>=10.1.0
beautifulsoup4>=4.12.0
lxml>=4.9.0
yt-dlp>=2024.1.0

# === Vision & OCR Intelligence ===
opencv-python-headless>=4.8.0
easyocr>=1.7.1

# === Torch CPU variant (Forces compact build, prevents massive CUDA download) ===
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.2.0+cpu; python_version < "3.13"
torch==2.12.0+cpu; python_version >= "3.13"

# === NLP/Vision model runtime ===
transformers>=4.40.0
accelerate>=0.27.0
safetensors>=0.4.3
