[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short --no-header
markers =
    unit: Unit tests (fast, no network)
    integration: Integration tests (may need running API)
    model: Model accuracy and calibration tests
